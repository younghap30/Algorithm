N(수열 개수), A(수열 리스트)
A 수열 리스트 채우기

for N만큼 반복:
	if 현재 수열값 >= 오름차순 자연수:
		while 현재 수열값 >= 오름차순 자연수:
			append()
			오름차순 자연수 1증가
			(+) 저장
	else 현재 수열값 < 오름차순 자연수:
		pop()
		if 스택 pop 결괏값 > 수열의 수:
			NO 출력
		else:
			(-) 저장

if NO 값을 출력한 적이 없으면:
	저장한 값 출력